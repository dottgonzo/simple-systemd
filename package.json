{
  "name": "simple-systemd",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "push": "git add -A && git commit -m 'fix' && git push",
    "hw": "git add -A && git commit -m 'oki' && npm version patch -m patch&&git add -A && git commit --amend -C HEAD&& git push&&npm publish",
    "patch": "npm version patch -m patch&&git add -A && git commit --amend -C HEAD&& git push&&npm publish",
    "prepublish": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dottgonzo/simple-systemd.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dottgonzo/simple-systemd/issues"
  },
  "dependencies": {
    "output-file-sync": "^1.1.1",
    "path-exists": "^2.1.0",
    "promise": "^7.0.4",
    "promised-exec": "^1.0.1",
    "verbo": "0.0.9"
  },
  "devDependencies": {
    "chai": "^3.4.1",
    "mocha": "^2.3.4"
  }
}
